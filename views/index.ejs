<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Search</title>
    <link rel="stylesheet" href="./public/index.css">
</head>
<body>
    <header class="header">
        <div class="company-name">Assignment 4</div>
        <nav class="nav-links">
            <ul>
                <li><a href="/index?lang=<%= lang %>"><%= lang === 'ru' ? 'Найти факт о числе' : 'Find fact about number' %></a></li>
                <li><a href="/currency?lang=<%= lang %>"><%= lang === 'ru' ? 'Информация о фильме' : 'Information about movie' %></a></li>
                <li><a href="/login?lang=<%= lang %>"><%= lang === 'ru' ? 'Выйти' : 'Log out' %></a></li>
                <form action="<%= lang === 'en' ? '/index?lang=en' : '/index?lang=ru' %>" class="form-inline ml-auto">
                    <div class="form-group">
                        <label for="langSelect" class="mr-2"style="color: white;"><%= lang === 'ru' ? 'Язык:' : 'Language:' %></label>
                        <select class="form-control" id="langSelect" name="lang" onchange="this.form.submit()">
                            <option value="en" <%= lang === 'en' ? 'selected' : '' %>>EN</option>
                            <option value="ru" <%= lang === 'ru' ? 'selected' : '' %>>RU</option>
                        </select>
                    </div>
                </form>
            </ul>
        </nav>
    </header>
    
    <div class="container">
        <h1><%= lang === 'ru' ? 'Найти факт о числе' : 'Find fact about number' %></h1>
        <form action="/index?lang=<%= lang %>" method="post">
            <input type="text" name="q" placeholder="<%= lang === 'ru' ? 'Введите число' : 'Enter a number' %>">
            <button type="submit"><%= lang === 'ru' ? 'Поиск' : 'Search' %></button>
        </form>
        <% if (number) { %>
            <div class="recipe">
                <h2><%= number %></h2>
                <ul>
                    <li><%= fact %></li>
                </ul>
            </div>
        <% } %>
    </div>

    <footer style="display: flex; justify-content: center; align-items: center; width: 100%; height: 70px; background-color: rgb(104, 104, 104);">
        Aibek Bazarbayev SE-2205
    </footer>
</body>
</html>
